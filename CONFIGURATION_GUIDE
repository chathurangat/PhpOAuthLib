In order to ru this module, you need to follow the instructions given below.

enable the CURL extension in apache server. use following command


    sudo apt-get install curl libcurl3 libcurl3-dev php5-curl

    After installing libcurl you should restart the web server with following command,

    sudo /etc/init.d/apache2 restart
